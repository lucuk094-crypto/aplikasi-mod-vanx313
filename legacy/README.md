# Arsip: situs statis lama

Folder ini berisi situs HTML statis versi lama (belum React).
Disimpan sebagai arsip saja — **tidak dipakai build & deploy**.

Situs aktif sekarang adalah aplikasi React + Vite di folder root repo
(`package.json`, `src/`, `public/`, `index.html`).
